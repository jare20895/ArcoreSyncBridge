# Glossary

- Sync Definition: A configuration that maps a Postgres table to SharePoint lists.
- Sync Ledger: A table that maps source rows to SharePoint items and stores hashes.
- Sharding Policy: Rules that route rows to specific lists.
- Provisioning: Creating lists and columns from Postgres schema.
- Delta Query: Graph API method to fetch incremental SharePoint changes.
