version: '3.8'

services:
  db:
    image: postgres:15-alpine
    restart: always
    command: postgres -c wal_level=logical
    environment:
      POSTGRES_USER: arcore
      POSTGRES_PASSWORD: arcore_password
      POSTGRES_DB: arcore_syncbridge
    ports:
      - "5465:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U arcore -d arcore_syncbridge"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: always
    ports:
      - "6384:6379"

  backend:
    build: ./backend
    restart: always
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8055:8000"
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env
    environment:
      - POSTGRES_USER=arcore
      - POSTGRES_PASSWORD=arcore_password
      - POSTGRES_DB=arcore_syncbridge
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - REDIS_URL=redis://redis:6379/0
      - SECRET_KEY=dev_secret_key
      - ALGORITHM=HS256
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started

  frontend:
    build: ./frontend
    restart: always
    command: npm run dev
    ports:
      - "3005:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - NEXT_PUBLIC_API_URL=http://arcore:8055
    depends_on:
      - backend

  celery_worker:
    build: ./backend
    restart: always
    command: celery -A app.worker.celery_app worker --loglevel=info --queues=sync_queue,default
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env
    environment:
      - POSTGRES_USER=arcore
      - POSTGRES_PASSWORD=arcore_password
      - POSTGRES_DB=arcore_syncbridge
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=postgresql://arcore:arcore_password@db:5432/arcore_syncbridge
      - SECRET_KEY=dev_secret_key
      - ALGORITHM=HS256
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started

  celery_beat:
    build: ./backend
    restart: always
    command: celery -A app.worker.celery_app beat --loglevel=info
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env
    environment:
      - POSTGRES_USER=arcore
      - POSTGRES_PASSWORD=arcore_password
      - POSTGRES_DB=arcore_syncbridge
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=postgresql://arcore:arcore_password@db:5432/arcore_syncbridge
      - SECRET_KEY=dev_secret_key
      - ALGORITHM=HS256
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started

  cdc_consumer:
    build: ./backend
    restart: always
    command: python -m app.worker.cdc_consumer_worker
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env
    environment:
      - POSTGRES_USER=arcore
      - POSTGRES_PASSWORD=arcore_password
      - POSTGRES_DB=arcore_syncbridge
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=postgresql://arcore:arcore_password@db:5432/arcore_syncbridge
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started

volumes:
  postgres_data: